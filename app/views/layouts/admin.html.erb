<!DOCTYPE html>
<html lang="<%= I18n.locale %>" dir="ltr" class="bg-stone-100">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= display_meta_tags site: "Experiment", noindex: true, nofollow: true, noarchive: true %>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body class="min-h-screen flex justify-center p-4 sm:p-8 text-stone-700 bg-stone-100">
    <%= render "admin/shared/flash" %>

    <main class="w-full max-w-4xl p-4 sm:p-8 bg-white ring-1 ring-stone-200 rounded-lg shadow-sm">
      <div class="flex items-center justify-between mb-4">
        <%= render "admin/shared/breadcrumbs" %>
        <%= render "admin/shared/current_user" %>
      </div>
      <%= yield %>
    </main>
  </body>
</html>
