<header class="page-header">
  <h1><%= title "Editing project" %></h1>
  <%= link_to "Show project", admin_project_path(@project), class: "ml-auto btn btn-primary" %>
</header>

<%= render "form", project: @project %>
